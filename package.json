{
  "name": "claude-peers-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "broker": "bun run broker.ts",
    "server": "bun run server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.1.0"
  }
}
