{
  "_comment": "Template only. Never commit a real token here - use .env.tokens (gitignored) or the /install bootstrap.",
  "mcpServers": {
    "claude-peers-cloud": {
      "command": "bun",
      "args": ["/ABSOLUTE/PATH/TO/claude-peers-cloud/server.ts"],
      "env": {
        "CLAUDE_PEERS_BROKER_URL": "https://claude-peers-cloud-production.up.railway.app",
        "CLAUDE_PEERS_TOKEN": "REPLACE_WITH_SHARED_TOKEN",
        "CLAUDE_PEERS_OWNER": "hamza"
      }
    }
  }
}
